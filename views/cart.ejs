<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/images/favicon.ico" type="image/x-icon">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
   
    <title>Uracline Space | Cart</title >
    <link rel="stylesheet" href="styles.css">
</head>
<body class="body" id="cart-body">
    <div class="container">
        <h1>Proceed to purchase</h1>
        <div class="selected-items">
            <%if(results.length > 0){%>
                <%results.forEach( item => { %>
                    <div class="selected-item">
                        <div class="cart-item-name"><%= item.item_name%></div>
                        <div class="cart-item-price">R<%= item.item_price%>.00</div>
                        <button id="payment-process" class="btn-quinary">Proceed To Payment</button>
                    </div>
                <%})%>
            <%} else {%>
                <p>Your cart is empty</p>
                <%}%>
        </div>
    </div>
</body>
</html>